{
    # Use environment variable for domain, default to localhost
    # Binds to internal ports 8080/8443 for rootless compatibility
    http_port 8080
    https_port 8443
    skip_install_trust
}

{$DOMAIN_NAME:localhost} {
    # Logging
    log {
        output file /var/log/caddy/access.log
        format json
    }

    # API Routes -> Controller
    # TODO: Revert this to route to web-interface and controller once implemented
    # handle_path /api/* {
    #    reverse_proxy controller:8000
    # }

    # All other routes -> Status Board (Temporary)
    handle {
        reverse_proxy status-board:8000
    }

    # Original Web Interface Routing (Disabled)
    # handle {
    #    reverse_proxy web-interface:8000
    # }
}
