[project]
name = "silvasonic-template"
version = "0.1.0"
description = "Silvasonic Template Service"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
