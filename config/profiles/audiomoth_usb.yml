---
# AudioMoth USB Microphone Profile
# Match via ALSA name fallback (Score 50) — exact VID/PID unknown.
schema_version: "1.0"
slug: audiomoth_usb
name: AudioMoth USB Microphone
description: AudioMoth running the USB Microphone App firmware (384kHz).
manufacturer: Open Acoustic Devices
model: AudioMoth
audio:
  sample_rate: 384000
  channels: 1
  format: S16LE
  match:
    alsa_name_contains: "AudioMoth"
processing:
  gain_db: 0.0
  chunk_size: 4096
stream:
  raw_enabled: true
  processed_enabled: true
  live_stream_enabled: false
  segment_duration_s: 10
