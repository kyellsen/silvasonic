# Silvasonic Environment Variables
# Copy this file to .env and adjust as needed

# --- CONTAINER ENGINE ---
# Podman-only (see ADR-0004, ADR-0013). No override needed.

# --- DEVELOPMENT MODE ---
# Set to 'True' for rich logging (colors, nice tracebacks)
# Set to 'False' for JSON logging (production/container aggregation)
SILVASONIC_DEVELOPMENT_MODE=True

# --- DATABASE ---
POSTGRES_USER=silvasonic
POSTGRES_PASSWORD=silvasonic
POSTGRES_DB=silvasonic
SILVASONIC_DB_PORT=5432

# --- SERVICE PORTS (Host-Exposed) ---
SILVASONIC_CONTROLLER_PORT=9100
# SILVASONIC_GATEWAY_HTTP_PORT=80     # Caddy (future)
# SILVASONIC_GATEWAY_HTTPS_PORT=443   # Caddy (future)

# --- PYTHON PATHS ---
SILVASONIC_REPO_PATH=/mnt/data/dev/apps/silvasonic
# Only needed if you have custom workspace locations
SILVASONIC_WORKSPACE_PATH=/mnt/data/dev_workspaces/silvasonic/

# --- PODMAN SOCKET ---
# Mounted into Controller for Tier 2 container management (see ADR-0013)
# Adjust user ID (1000) if necessary
SILVASONIC_PODMAN_SOCKET=/run/user/1000/podman/podman.sock

# --- NETWORK ---
# Network name for Tier 2 containers (must match compose.yml network name)
SILVASONIC_NETWORK=silvasonic-net

# Required for Testcontainers to find Podman
# DOCKER_HOST=unix:///run/user/1000/podman/podman.sock
